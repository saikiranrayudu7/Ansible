- name: catalogue
  hosts: catalogue
  become: yes

  tasks:
  - name: Install nodejs
    ansible.builtin.dnf:
      name: nodejs18
      state: present

  - name: create roboshop user
    ansible.builtin.user:
      name: roboshop

  - name: create app directory
    ansible.builtin.file:
      path: /app
      state: directory